[project]
name = "edge-equation-v1"
version = "0.1.0"
description = "Edge Equation sports analytics engine"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
